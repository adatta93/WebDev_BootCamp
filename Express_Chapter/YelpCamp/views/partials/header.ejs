<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <link rel="icon" href="/img/nodejs.png" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/css/bootstrap-4.3.1.min.css">
    <link rel="stylesheet" href="/css/font-awesome-5.11.1-all.css">
    <title>YelpCamp</title>
</head>
<style>
    body{
        padding-top: 70px;
        background-color: #112233;
    }
    figure img {
        height: 200px !important;
        width: 100% !important;
        object-fit: cover;
    }
    .camp-delete {
        display: inline;
    }
    .btn-xs {
        padding: .25rem .4rem;
        font-size: .875rem;
        line-height: 1.1;
        border-radius: .2rem;
    }
    .btn-danger{
        background-color: #d70015;
        border-color: #d70015;
    }
    .nav-bg-custom{
        background: #000e1d;
        box-shadow: 0px 2px 10px 1px #2c455f;
    }
    .login-card{
        height: 80vh;
        border: 2px solid #007bff;
        box-shadow: 0 0 10px 1px black, 0 0 10px 3px rgba(0,0,0,0.5);
    }
    .register-card{
        height: 80vh;
        border: 2px solid #d39e00;
        box-shadow: 0 0 10px 1px black, 0 0 10px 3px rgba(0,0,0,0.5);
    }
    .jumbotron{
        background-color: #a4c0ff;
    }
</style>

<body>
    <nav class="navbar navbar-expand-md navbar-dark fixed-top nav-bg-custom">
        <a class="navbar-brand" href="/">YelpCamp</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navLinks"
            aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navLinks">
            <ul class="navbar-nav ml-auto">
                <% if(!currentUser){ %>
                <li class="nav-item"><a href="/login" class="nav-link">Login</a></li>
                <li class="nav-item"><a href="/register" class="nav-link">Register</a></li>
                <% } else{ %>
                <li class="nav-item"><a class="nav-link">Welcome <%= currentUser.username %></a></li>
                <li class="nav-item"><a href="/logout" class="nav-link">Logout</a></li>
                <% } %>
            </ul>
        </div>
    </nav>
    <div class="container">
        <% if(error && error.length > 0){ %>
        <div class="alert alert-danger"><%= error %></div>
        <% } %>
        <% if(success && success.length > 0){ %>
        <div class="alert alert-success"><%= success %></div>
        <% } %>
    </div>